# V5: 让一部分样本跨过 0°，进入反向关门角区间。
# 目标是不再只是“关得不够”，而是明确制造一批朝错误方向走的 failure。

defaults:
  - level1_close_door_noise_v4
  - _self_

name: Level1_close_door_noise_v5

noise:
  end_effector_euler_deg: [-20.0, 20.0]
  door_close_angle_deg: [-95.0, -25.0]
