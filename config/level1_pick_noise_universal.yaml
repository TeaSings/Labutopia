# Pick 任务 - 通用 VLM 采集（所有动作统一方案）
defaults:
  - level1_pick
  - universal_vlm_collect
  - _self_

name: Level1_pick_noise_universal
max_episodes: 50

pick:
  pre_offset_x: 0.05
  pre_offset_z: 0.12
  after_offset_z: 0.25
  end_effector_euler_deg: [0.0, 90.0, 25.0]

noise:
  enabled: true
